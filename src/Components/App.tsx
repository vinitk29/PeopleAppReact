import React from "react";
import MainContainer from "./MainContainer";

const App = (): React.ReactElement => {
    return (
        <div className="app">
            <MainContainer/>
        </div>
    )
}

export default App;